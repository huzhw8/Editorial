<footer>
    <nav class="pagination" role="navigation">
        {{#if prev}}
        <a class="page previous newer-posts" href="{{page_url prev}}">最近发表</a>
        {{/if}}
        <span class="page-number">页数 {{page} of {{pages}}</span>
        {{#if next}}
        <a class="page next older-posts" href="{{page_url next}}">往日发表</a>
        {{/if}}
    </nav>
</footer>
